mysql 데이터베이스에서 리스트전체를 꺼내올려는데 autonincrement 로 설정을 해놓았는데
삭제를 하니까 번호가 꼬였다 데이터베이스 내에서는 어떻게처리하는지 알지만 
mybatis 에서 sql을 쏠려고하니까 어떻게해야하는지 굉장히 헤맸는데 SET @COUNT = 0;이거를 처음 보내고
UPDATE test SET  num = @COUNT:=@COUNT+1; 이거를 보내고 난후에 리스트를 출력하는걸로 만들었는데
구글에아무리쳐도 잘모르겠어서 그냥 생각나는 내방식대로 구현을 한건데 결과는 나오지만 쓸데없는 메모리낭비가아닌가 싶기도하다..
